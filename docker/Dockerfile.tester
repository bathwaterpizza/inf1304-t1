# Dockerfile for cluster verification and testing
FROM python:3.11-slim

WORKDIR /app

# Install required packages
RUN pip install confluent-kafka

# Copy verification script
COPY scripts/verify_kafka_cluster.py .

# Make script executable
RUN chmod +x verify_kafka_cluster.py

# Default command
CMD ["python", "verify_kafka_cluster.py"]